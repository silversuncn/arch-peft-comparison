# Analysis-only requirements (Path A: reproduce statistics and figures from provided data).
# No GPU or model downloads needed. Python 3.11 (tested on 3.11.15).
# Pinned to the versions used to produce the published results.
numpy==2.4.4
pandas==3.0.1
scipy==1.17.1
statsmodels==0.14.6
matplotlib==3.10.8
